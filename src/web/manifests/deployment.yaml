apiVersion: apps/v1
kind: Deployment
metadata:
  name: "web"
  labels:
    app: "web"
spec:
  replicas: 1
  selector:
    matchLabels:
      app: "web"
  template:
    metadata:
      labels:
        app: "web"
    spec:
      containers:
        - name: node
          image: criwjxfqayhhqse.azurecr.io/chaos-demo/web-chaos-demo:azd-deploy-1738158250
          ports:
            - containerPort: 5000
          env:
            - name: APP_PORT
              value: "8080"
            - name: APP_VERSION
              value: "1.0.0"
            - name: AZURE_CLIENT_ID
              value: ${AZURE_AKS_IDENTITY_CLIENT_ID}
          imagePullPolicy: Always
          resources:
            limits:
              cpu: "512m"
              memory: "512Mi"
            requests:
              cpu: "100m"
              memory: "128Mi"

apiVersion: v1
kind: Service
metadata:
  name: web

spec:
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: 5000
  selector:
    app: web